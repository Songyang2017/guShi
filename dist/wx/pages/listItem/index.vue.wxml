<template name="4be99ecc">
  <view wx:if="{{list.length}}" class="_div data-v-0cac13a1">
    <view wx:key="its._id" key="{{its._id}}" class="_div data-v-0cac13a1 card" wx:for="{{list}}" wx:for-index="index" wx:for-item="its">
      <view class="_p data-v-0cac13a1 title">{{its.title || its.rhythmic || its.chapter}}</view>
      <view class="_p data-v-0cac13a1 author" wx:if="{{its.author}}">{{its.author}}</view>
      <view class="_p data-v-0cac13a1 p-content">{{its.paragraphs[0] || its.content[0]}}{{its.paragraphs[1] || its.content[1]}}{{its.paragraphs[2] || its.content[2]}}</view>
    </view>
  </view>
</template>